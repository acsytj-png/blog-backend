{
  "name": "blog-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "install": "npm install mongodb", // 强制安装mongodb包
    "start": "node server.js"         // 启动命令
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.2.0",
    "mongodb": "^6.3.0" // 显式声明mongodb依赖
  }
}